# direnv hook: ensure the project root is on PATH for jumpscript CLI.
repo_dir="${PWD}"

case ":${PATH}:" in
	*":${repo_dir}:"*) ;;
	*) export PATH="${repo_dir}/bin:${PATH}" ;;
esac

export JUMPSCRIPT_ROOT="${repo_dir}"
